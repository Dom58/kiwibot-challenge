class Bot {
    constructor(
        bot_id,
        delivery_id,
    ) {
        this.bot_id = bot_id; // referenced to bots
        this.delivery_id = delivery_id; // referenced to deliveries
    }
}

export default Bot;
